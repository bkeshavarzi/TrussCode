#include <iostream>
#include "Node.h"
#include <vector>
#include "Material.h"
#include "Element.h"
#include "ReadText.h"
#include "GlobalStiffness.h"

using namespace std;

int main()
{
     //vector <Node> NV=ReadNodeFile();
    //vector <Element> EV=ReadElementFile(NV);
    //MatrixXd KG=MakeGlobalStiffnessMatrix(NV,EV);
    //NV[0].GetRes();
    //NV[0].getcord();
    bool x=1;
    if (x==true)
    {
        cout << x <<endl;
    }
}
